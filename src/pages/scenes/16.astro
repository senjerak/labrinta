---
import StoryLayout from "../../layouts/StoryLayout.astro";
import "../../styles/novel.css";
---

<StoryLayout>
  <script is:inline>
    preloadImages("/s_one/", [
      "roomone_dark1.gif",
      "CASCA_sit3.gif",
      "CASPER_talkL.gif",
    ]);

    waitForNovel(function prepareNovel() {
      novel.imagePath = "/s_one/"; // path to your image directory
      novel.audioPath = ""; // path to your audio directory

      /**
       * Characters
       */
      const n = new Character("");
      const casper = new Character("Casper", {
        color: "rgba(218, 234, 127, 1)",
      });
      const casca = new Character("Casca", { color: "rgb(230, 145, 185)" });

      /**
       * Positions
       */
      const cascaroom2 = new Position(45, 160, 0, 0);
      const casperlobby4 = new Position(540, 300, 0, 0);
      /**
       * Scene Sixteen - Room One Dark P1
       *
       */
      script = [
        label,
        "start",
        scene,
        "roomone_dark1.gif",

        casca,
        { image: `CASCA_sit3.gif?A=${Date.now()}`, position: cascaroom2 },

        casper,
        { image: "CASPER_talkL.gif", position: casperlobby4 },

        casper,
        "huh. . ?",

        jsCall,
        { fcn: changeScene(17) },
      ];
    });
  </script>
</StoryLayout>
